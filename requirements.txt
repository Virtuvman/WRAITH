# CAMWATCH — Python Dependencies
# Install all at once: pip install -r requirements.txt

streamlit>=1.35.0
pandas>=2.0.0
plotly>=5.20.0
# Pin folium/branca pair to avoid known HeatMap serialization regressions.
folium==0.17.0
branca==0.7.2
streamlit-folium>=0.20.0
python-dotenv>=1.0.0
shodan>=1.31.0
mgrs>=1.4.0
apscheduler>=3.10.0
