# CLAUDE.md — WRAITH

See global machine instructions at `~/.claude/CLAUDE.md`.

## Project-Specific Notes

- Entry point: `app.py` — `streamlit run app.py`
- Dependency pin: `folium==0.17.0` + `branca==0.7.2` (do not upgrade — HeatMap serialization regression risk)
- Sample data: `data/poc_global_500.csv` and variants for visual QA
- Docs: `WRAITH_MASTER_PLAN.md`, `docs/MASTER_PROMPT.md`

## Change Protocol

New features and scope changes go through PAUL or SEED — not inline instructions.
